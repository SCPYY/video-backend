<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.module.content.mapper.EpisodeMapper">

    <!-- 批量插入剧集 -->
    <insert id="batchInsert" parameterType="list">
        INSERT INTO episodes (content_id, episode_number, title, video_url, duration,
            is_free, access_type, price_platform_coin, sort_order, source_type, transcode_status, original_filename, file_size, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.contentId}, #{item.episodeNumber}, #{item.title}, #{item.videoUrl},
             #{item.duration}, #{item.isFree}, #{item.accessType}, #{item.pricePlatformCoin}, #{item.sortOrder}, #{item.sourceType},
             #{item.transcodeStatus}, #{item.originalFilename}, #{item.fileSize}, #{item.createdAt})
        </foreach>
    </insert>

    <!-- 软删除指定内容的所有剧集 -->
    <update id="softDeleteByContentId">
        UPDATE episodes SET sort_order = -1 WHERE content_id = #{contentId}
    </update>

    <!-- 统计某内容的剧集数量 -->
    <select id="countByContentId" resultType="int">
        SELECT COUNT(*) FROM episodes WHERE content_id = #{contentId} AND sort_order >= 0
    </select>

</mapper>
