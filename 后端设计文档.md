# 海外短剧+影游独立站——后端设计文档 (v1.2)

---

## 1. 文档概述

### 1.1 项目背景

本项目是一个面向海外市场的短剧+影游独立站，核心功能包括：用户观看短剧、体验互动影游、通过海外支付渠道（PayPal/Stripe）付费解锁内容，以及后台内容管理。

### 1.2 技术栈概览

| 层级 | 技术选型 | 说明 |
| :--- | :--- | :--- |
| **开发语言** | Java 17 | LTS版本，稳定且生态成熟 |
| **核心框架** | Spring Boot 3.x | 企业级应用开发基础框架 |
| **安全框架** | Spring Security + JWT | 认证授权，无状态会话管理 |
| **持久层** | MyBatis-Plus | 简化CRUD，支持灵活SQL |
| **数据库** | MySQL 8.0 + Redis 7.0 | 主数据存储 + 缓存/会话存储 |
| **API文档** | Knife4j (Swagger增强) | 自动生成接口文档，方便前后端联调 |
| **构建工具** | Maven | 依赖管理和项目构建 |
| **容器化** | Docker / Docker Compose | 环境一致性，简化部署 |

---

## 2. 系统架构设计

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│ 前端 (Nuxt.js) │
│ (用户端 Web + 管理端) │
└───────────────────────────┬─────────────────────────────────────┘
│ HTTPS / RESTful API
┌───────────────────────────▼─────────────────────────────────────┐
│ Nginx (反向代理 + 负载均衡) │
│ (SSL终止 / 静态资源) │
└───────────────────────────┬─────────────────────────────────────┘
│
┌───────────────────────────▼─────────────────────────────────────┐
│ Spring Boot 应用集群 │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ Controller Layer (表现层) │ │
│ │ - 接收请求、参数校验、响应封装 │ │
│ ├──────────────────────────────────────────────────────────┤ │
│ │ Service Layer (业务逻辑层) │ │
│ │ - 核心业务逻辑、事务管理、缓存操作 │ │
│ ├──────────────────────────────────────────────────────────┤ │
│ │ Mapper Layer (数据访问层) │ │
│ │ - 数据库CRUD操作 │ │
│ └──────────────────────────────────────────────────────────┘ │
│ │
│ 核心组件: Spring Security (JWT) / 全局异常处理 / 支付模块 │
└───────────────────────┬──────────────────┬─────────────────────┘
│ │
┌───────────────▼──────┐ ┌───────▼───────────────┐
│ MySQL 8.0 │ │ Redis 7.0 │
│ (主数据存储) │ │ (缓存/会话/分布式锁) │
└──────────────────────┘ └───────────────────────┘
│
┌───────────────▼──────────────────────────────────────┐
│ 外部服务集成 │
│ - PayPal API / Stripe API (海外支付) │
│ - AWS S3 / 阿里云OSS (视频存储) │
│ - CDN (视频加速分发) │
└──────────────────────────────────────────────────────┘
```

### 2.2 分层架构说明

采用经典的三层架构模式，职责清晰，便于维护和测试：

| 层级 | 职责 | 示例 |
| :--- | :--- | :--- |
| **Controller层** | 接收HTTP请求，参数校验，调用Service，封装统一响应返回 | `UserController`、`OrderController` |
| **Service层** | 核心业务逻辑处理，事务管理，调用Mapper进行数据操作 | `UserService`、`PaymentService` |
| **Mapper层** | 数据库CRUD，SQL映射 | `UserMapper`、`OrderMapper` |

---

## 3. 功能模块划分

### 3.1 模块清单

根据1.0版本需求，后端分为以下核心模块：

| 模块 | 包路径 | 核心功能 | 优先级 |
| :--- | :--- | :--- | :--- |
| **用户模块** | `com.project.module.user` | 注册、登录、JWT认证、个人信息管理 | P0 |
| **内容模块** | `com.project.module.content` | 短剧/影游元数据管理、剧集/关卡管理、状态控制 | P0 |
| **商品模块** | `com.project.module.product` | 定价方案管理（解锁/会员） | P1 |
| **订单模块** | `com.project.module.order` | 订单生成、查询、状态管理 | P0 |
| **支付模块** | `com.project.module.payment` | PayPal/Stripe对接、Webhook处理 | P0 |
| **权益模块** | `com.project.module.entitlement` | 用户购买后的权益管理（解锁/会员期） | P0 |
| **管理后台API** | `com.project.module.admin` | 管理员认证、权限、运营管理、文件上传、批量剧集管理、操作审计 | P0 |
| **评论与弹幕模块** | `com.project.module.social` | 剧集评论、多级回复、弹幕发送与查询、点赞互动 | P1 |

### 3.2 模块依赖关系

```
用户模块 ← 订单模块 ← 支付模块
↑                      ↑
内容模块 ← 商品模块 ← 评论与弹幕模块
↑
权益模块（用户购买后获得内容访问权限）
```

---

## 4. 数据库设计

### 4.1 数据库选型说明

- **MySQL 8.0**：核心业务数据存储，成熟稳定，与Java生态集成良好
- **Redis 7.0**：缓存热点数据（如热门短剧列表）、存储用户会话（JWT Token黑名单）、分布式锁（防止支付并发问题）

### 4.2 E-R 关系图（核心表关系）

```
┌─────────────────┐     ┌─────────────────┐
│ sys_user │ │ user_entitlement│
│─────────────────│ │─────────────────│
│ id (PK) │◄─────────│ user_id (FK) │
│ username │ │ content_id (FK) │
│ email │ │ type │
│ password_hash │ │ expire_time │
│ ... │ └─────────────────┘
└────────┬────────┘ ▲
│ │
│ ┌──────────────────────────┘
│ │
│ │ ┌─────────────────┐
│ └────│ orders │
│ │─────────────────│
│ │ id (PK) │
│ │ order_no │
│ │ user_id (FK) │───────┐
│ │ product_id (FK) │ │
│ │ amount │ │
│ │ status │ │
│ └─────────────────┘ │
│ │
▼ ▼
┌─────────────────┐ ┌─────────────────┐
│ contents │ │ products │
│─────────────────│ │─────────────────│
│ id (PK) │◄─────────│ content_id (FK) │
│ title │ │ name │
│ type (1/2) │ │ price_usd │
│ cover_url │ │ price_eur │
│ status │ │ type (解锁/会员) │
│ ... │ └─────────────────┘
└────────┬────────┘
│
▼
┌─────────────────┐
│ episodes │
│─────────────────│
│ id (PK) │
│ content_id (FK) │
│ episode_number │
│ video_url │
│ interactive_cfg │ (JSON)
│ is_free │
└─────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ content_extras │
│─────────────────────────────────────────────────────────────│
│ id (PK) | content_id (FK) | key (配置项) | value (JSON) │
│─────────────────────────────────────────────────────────────│
│ 示例数据: │
│ content_id=1, key='director', value='"张三"' │
│ content_id=2, key='interactive_tree', value='{...}' │
│ content_id=2, key='game_engine', value='"Unity"' │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ 评论与弹幕模块（社交互动）                                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────┐     ┌─────────────────┐                     │
│  │ comments         │     │ danmaku          │                    │
│  │─────────────────│     │─────────────────│                     │
│  │ id (PK)          │     │ id (PK)          │                    │
│  │ content_id (FK)  │     │ episode_id (FK)  │                    │
│  │ episode_id (FK)  │     │ user_id (FK)     │                    │
│  │ user_id (FK)     │     │ content          │                    │
│  │ parent_id         │     │ video_time       │                    │
│  │ root_id           │     │ color            │                    │
│  │ reply_to_user_id │     │ position         │                    │
│  │ content           │     │ like_count       │                    │
│  │ like_count        │     │ status           │                    │
│  │ reply_count       │     └─────────────────┘                    │
│  │ status            │                                           │
│  └────────┬──────────┘                                           │
│           │                                                      │
│  ┌────────▼──────────┐     ┌─────────────────┐                   │
│  │ comment_likes      │     │ danmaku_likes    │                  │
│  │───────────────────│     │─────────────────│                   │
│  │ id (PK)            │     │ id (PK)          │                  │
│  │ comment_id (FK)   │     │ danmaku_id (FK)  │                  │
│  │ user_id (FK)       │     │ user_id (FK)     │                  │
│  │ type (1点赞/2点踩) │     └─────────────────┘                   │
│  └───────────────────┘                                           │
│                                                                  │
│  ┌─────────────────┐                                             │
│  │ comment_reports   │  (举报表，管理端使用)                       │
│  │─────────────────│                                             │
│  │ id (PK)           │                                            │
│  │ comment_id (FK)  │                                            │
│  │ user_id (FK)      │                                            │
│  │ reason            │                                            │
│  │ status (0待处理)   │                                            │
│  └─────────────────┘                                             │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ admin_logs (管理员操作审计)                                     │
│──────────────────────────────────────────────────────────────│
│ id (PK) | admin_id (FK→sys_admin) | action | module |         │
│ target_id | before_data (JSON) | after_data (JSON) |          │
│ ip_address | user_agent | created_at                           │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 核心数据表设计

#### 4.3.1 用户表 (sys_user)

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `username` | VARCHAR(64) | UNIQUE, NOT NULL | 用户名 |
| `email` | VARCHAR(128) | UNIQUE | 邮箱 |
| `phone` | VARCHAR(32) | | 手机号（可选） |
| `password_hash` | VARCHAR(255) | NOT NULL | BCrypt加密密码 |
| `nickname` | VARCHAR(64) | | 昵称 |
| `avatar_url` | VARCHAR(255) | | 头像URL |
| `status` | TINYINT | DEFAULT 0 | 状态：0-正常 1-禁用 |
| `last_login_time` | DATETIME | | 最后登录时间 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 注册时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引**：`idx_email`、`idx_username`

#### 4.3.2 内容主表 (contents)

存储所有内容（短剧/影游）的共有字段。

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `type` | TINYINT | NOT NULL | 类型：1-短剧 2-影游 |
| `title` | VARCHAR(255) | NOT NULL | 标题 |
| `description` | TEXT | | 简介 |
| `cover_url` | VARCHAR(255) | | 封面图URL |
| `category` | VARCHAR(64) | | 分类 |
| `tags` | VARCHAR(255) | | 标签（逗号分隔） |
| `status` | TINYINT | DEFAULT 0 | 状态：0-下架 1-上架 |
| `view_count` | BIGINT | DEFAULT 0 | 观看次数（可定时更新） |
| `sort_order` | INT | DEFAULT 0 | 排序权重（后台管理用） |
| `admin_remark` | VARCHAR(255) | | 管理员备注 |
| `created_by` | BIGINT | FK → sys_admin.id | 创建人（管理员ID） |
| `updated_by` | BIGINT | FK → sys_admin.id | 最后更新人（管理员ID） |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引**：`idx_status`、`idx_type`、`idx_category`

#### 4.3.3 内容扩展表 (content_extras)

以键值对形式存储各类内容的特有属性，实现灵活扩展。

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `content_id` | BIGINT | NOT NULL, FK → contents.id | 关联内容ID |
| `key` | VARCHAR(64) | NOT NULL | 配置项标识 |
| `value` | JSON | | 配置值（JSON格式） |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：`uk_content_key` (content_id, key) UNIQUE

**各内容类型约定的 key 清单**：

| 内容类型 | key | value 示例 | 说明 |
| :--- | :--- | :--- | :--- |
| 短剧 (type=1) | `director` | `"张三"` | 导演 |
| 短剧 (type=1) | `actor` | `["李四", "王五"]` | 演员列表 |
| 短剧 (type=1) | `release_year` | `2025` | 上映年份 |
| 影游 (type=2) | `interactive_tree` | `{"node_1": {...}}` | 互动剧情树 |
| 影游 (type=2) | `game_engine` | `"Unity"` | 游戏引擎 |
| 影游 (type=2) | `min_players` | `1` | 最少玩家数 |
| 通用扩展 | `seo_keywords` | `"短剧,海外,悬疑"` | SEO关键词 |
| 通用扩展 | `trailer_url` | `"https://..."` | 预告片URL |

#### 4.3.4 剧集/关卡表 (episodes)

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `content_id` | BIGINT | NOT NULL, FK → contents.id | 关联内容ID |
| `episode_number` | INT | NOT NULL | 集数/关卡编号 |
| `title` | VARCHAR(255) | | 剧集标题 |
| `video_url` | VARCHAR(255) | | 视频URL（CDN地址） |
| `duration` | INT | | 时长（秒） |
| `interactive_config` | JSON | | 影游互动配置（分支/选项数据） |
| `is_free` | TINYINT | DEFAULT 0 | 是否免费：0-付费 1-免费 |
| `sort_order` | INT | DEFAULT 0 | 排序 |
| `source_type` | TINYINT | DEFAULT 1 | 视频来源：1-本地上传 2-第三方URL |
| `transcode_status` | TINYINT | DEFAULT 0 | 转码状态：0-未转码 1-转码中 2-已完成 3-失败 |
| `original_filename` | VARCHAR(255) | | 原始文件名 |
| `file_size` | BIGINT | | 文件大小（字节） |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：`idx_content_id`

#### 4.3.5 商品表 (products)

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `type` | TINYINT | NOT NULL | 类型：1-单集解锁 2-全集解锁 3-会员 |
| `content_id` | BIGINT | FK → contents.id | 关联内容ID（解锁类商品需要） |
| `episode_id` | BIGINT | FK → episodes.id | 关联剧集ID（单集解锁时使用） |
| `name` | VARCHAR(128) | NOT NULL | 商品名称 |
| `price_usd` | DECIMAL(10,2) | | 美元价格 |
| `price_eur` | DECIMAL(10,2) | | 欧元价格 |
| `duration_days` | INT | | 会员有效期天数（会员类商品） |
| `status` | TINYINT | DEFAULT 1 | 状态：0-下架 1-上架 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：`idx_content_id`、`idx_status`

#### 4.3.6 订单表 (orders)

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `order_no` | VARCHAR(64) | UNIQUE, NOT NULL | 订单号 |
| `user_id` | BIGINT | NOT NULL, FK → sys_user.id | 用户ID |
| `product_id` | BIGINT | NOT NULL, FK → products.id | 商品ID |
| `amount` | DECIMAL(10,2) | NOT NULL | 支付金额 |
| `currency` | VARCHAR(8) | NOT NULL | 币种（USD/EUR） |
| `payment_method` | VARCHAR(32) | | 支付方式（PAYPAL/STRIPE） |
| `status` | TINYINT | DEFAULT 0 | 状态：0-待支付 1-已支付 2-已取消 3-已退款 |
| `gateway_order_id` | VARCHAR(128) | | 支付网关订单ID |
| `gateway_tx_id` | VARCHAR(128) | | 支付网关交易流水号 |
| `paid_at` | DATETIME | | 支付时间 |
| `expired_at` | DATETIME | | 订单过期时间（超时未付自动取消） |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引**：`idx_order_no`、`idx_user_id`、`idx_status`、`idx_gateway_order_id`

#### 4.3.7 用户权益表 (user_entitlements)

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `user_id` | BIGINT | NOT NULL, FK → sys_user.id | 用户ID |
| `type` | TINYINT | NOT NULL | 类型：1-内容解锁 2-会员 |
| `content_id` | BIGINT | FK → contents.id | 解锁的内容ID（type=1时） |
| `episode_id` | BIGINT | FK → episodes.id | 解锁的剧集ID（单集解锁时） |
| `expire_time` | DATETIME | | 到期时间（NULL表示永久） |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：`idx_user_id`、`idx_content_id`、`idx_expire_time`

#### 4.3.8 管理员表 (sys_admin)

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `username` | VARCHAR(64) | UNIQUE, NOT NULL | 管理员账号 |
| `password_hash` | VARCHAR(255) | NOT NULL | BCrypt加密密码 |
| `role` | VARCHAR(32) | DEFAULT 'EDITOR' | 角色：ADMIN/EDITOR/VIEWER |
| `status` | TINYINT | DEFAULT 0 | 状态：0-正常 1-禁用 |
| `last_login_time` | DATETIME | | 最后登录时间 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 4.3.9 评论表 (comments)

支持多级嵌套回复（楼中楼），通过 `parent_id` + `root_id` 实现无限级嵌套。

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `content_id` | BIGINT | NOT NULL, FK → contents.id | 关联内容ID |
| `episode_id` | BIGINT | FK → episodes.id | 剧集ID，NULL表示整剧评论 |
| `user_id` | BIGINT | NOT NULL, FK → sys_user.id | 评论用户ID |
| `parent_id` | BIGINT | DEFAULT 0 | 父评论ID，0表示一级评论 |
| `root_id` | BIGINT | DEFAULT 0 | 根评论ID，用于快速查询整个评论树 |
| `reply_to_user_id` | BIGINT | FK → sys_user.id | 回复的目标用户ID（@某人） |
| `content` | TEXT | NOT NULL | 评论内容（最多500字） |
| `like_count` | INT | DEFAULT 0 | 点赞数 |
| `dislike_count` | INT | DEFAULT 0 | 点踩数 |
| `reply_count` | INT | DEFAULT 0 | 子回复数量（冗余，加速展示） |
| `status` | TINYINT | DEFAULT 1 | 状态：1-正常 2-已删除 3-审核中 |
| `ip_address` | VARCHAR(64) | | 发布时的IP（风控用） |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引**：`idx_content_episode (content_id, episode_id, parent_id)`、`idx_root_id (root_id, created_at)`、`idx_user_id`

**设计要点**：
- `episode_id` 为 NULL 表示"整剧评论"，有值表示"单集评论"，以此区分两种评论场景
- `parent_id` 为 0 表示一级评论，>0 表示回复某条评论
- `root_id` 存储根评论ID，一次查询即可获取整个评论树，无需递归DB
- `reply_count` 为冗余字段，避免每次展示评论时都 COUNT 子回复

#### 4.3.10 弹幕表 (danmaku)

存储视频弹幕数据，按视频时间点分段查询。

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `episode_id` | BIGINT | NOT NULL, FK → episodes.id | 剧集/关卡ID |
| `user_id` | BIGINT | NOT NULL, FK → sys_user.id | 发送用户ID |
| `content` | VARCHAR(200) | NOT NULL | 弹幕内容（最多200字） |
| `video_time` | INT | NOT NULL | 弹幕出现的视频时间点（秒） |
| `color` | VARCHAR(7) | DEFAULT '#FFFFFF' | 颜色（十六进制） |
| `position` | TINYINT | DEFAULT 0 | 位置：0-滚动 1-顶部 2-底部 |
| `font_size` | TINYINT | DEFAULT 2 | 字号：1-小 2-中 3-大 |
| `like_count` | INT | DEFAULT 0 | 点赞数 |
| `status` | TINYINT | DEFAULT 1 | 状态：1-正常 2-已删除 3-审核中 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：`idx_episode_time (episode_id, video_time)`、`idx_user_id`

**设计要点**：
- 弹幕按 `video_time` 分段查询，前端播放器根据当前播放进度请求对应时间段的弹幕
- 使用 Redis 缓存弹幕数据（30秒过期），减少高频查询对DB的压力
- 单用户发送频率限制：3条/10秒

#### 4.3.11 评论点赞表 (comment_likes)

记录用户对评论的点赞/点踩，保证幂等性。

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `comment_id` | BIGINT | NOT NULL, FK → comments.id | 评论ID |
| `user_id` | BIGINT | NOT NULL, FK → sys_user.id | 用户ID |
| `type` | TINYINT | NOT NULL | 1-点赞 2-点踩 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**约束**：`uk_comment_user (comment_id, user_id)` UNIQUE — 同一用户对同一评论只有一条记录

**设计要点**：
- 重复相同操作 → 取消（删除记录，计数-1）
- 切换操作（点赞→点踩）→ 更新 type，计数联动变更

#### 4.3.12 弹幕点赞表 (danmaku_likes)

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `danmaku_id` | BIGINT | NOT NULL, FK → danmaku.id | 弹幕ID |
| `user_id` | BIGINT | NOT NULL, FK → sys_user.id | 用户ID |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**约束**：`uk_danmaku_user (danmaku_id, user_id)` UNIQUE

#### 4.3.13 评论举报表 (comment_reports)

供管理端处理违规评论。

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `comment_id` | BIGINT | NOT NULL, FK → comments.id | 被举报评论ID |
| `user_id` | BIGINT | NOT NULL, FK → sys_user.id | 举报人ID |
| `reason` | VARCHAR(255) | NOT NULL | 举报原因 |
| `status` | TINYINT | DEFAULT 0 | 0-待处理 1-已处理 2-驳回 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：`idx_comment_id`、`idx_status`

#### 4.3.14 管理员操作日志表 (admin_logs)

记录管理员的关键操作，便于审计和问题追溯。

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 主键 |
| `admin_id` | BIGINT | NOT NULL, FK → sys_admin.id | 操作管理员ID |
| `action` | VARCHAR(64) | NOT NULL | 操作类型：CREATE/UPDATE/DELETE/UPLOAD/BATCH_CREATE |
| `module` | VARCHAR(64) | NOT NULL | 操作模块：CONTENT/EPISODE/UPLOAD/IMAGE |
| `target_id` | VARCHAR(64) | | 操作目标ID |
| `before_data` | JSON | | 修改前数据快照 |
| `after_data` | JSON | | 修改后数据快照 |
| `ip_address` | VARCHAR(64) | | 操作IP地址 |
| `user_agent` | VARCHAR(255) | | 浏览器User-Agent |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 操作时间 |

**索引**：`idx_admin_id`、`idx_created_at`、`idx_module_target (module, target_id)`

**用途**：
- 审计追溯：谁在什么时间做了什么操作
- 误操作恢复：通过 `before_data` 快照回滚数据
- 安全监控：检测异常操作模式（如同一IP短时间大量删除）

---

## 5. API接口设计规范

### 5.1 统一响应格式

所有API采用统一的JSON响应格式：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": { ... },
  "timestamp": 1704067200000
}
```

**状态码定义：**

| code | 含义 |
| :--- | :--- |
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未认证/Token失效 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 业务冲突（如重复购买） |
| 500 | 服务器内部错误 |

### 5.2 请求规范

- **请求方式**：遵循RESTful规范，使用GET/POST/PUT/DELETE
- **请求头**：认证接口需携带 `Authorization: Bearer <token>`
- **数据格式**：`Content-Type: application/json`

### 5.3 核心API端点清单

| 模块 | Method | Endpoint | 功能 | 认证 |
| :--- | :--- | :--- | :--- | :--- |
| 用户 | POST | `/api/v1/auth/register` | 用户注册 | 否 |
| | POST | `/api/v1/auth/login` | 用户登录 | 否 |
| | POST | `/api/v1/auth/refresh` | 刷新Token | 是 |
| | POST | `/api/v1/auth/logout` | 登出 | 是 |
| | GET | `/api/v1/user/profile` | 获取个人信息 | 是 |
| | PUT | `/api/v1/user/profile` | 更新个人信息 | 是 |
| 内容 | GET | `/api/v1/contents` | 内容列表（分页，支持筛选） | 否 |
| | GET | `/api/v1/contents/{id}` | 内容详情（含扩展属性） | 否 |
| | GET | `/api/v1/contents/{id}/episodes` | 获取剧集列表 | 否 |
| | GET | `/api/v1/episodes/{id}/play` | 获取播放信息（含鉴权） | 是 |
| | GET | `/api/v1/contents/hot` | 热门内容 | 否 |
| 商品 | GET | `/api/v1/products` | 商品列表 | 否 |
| | GET | `/api/v1/products/{id}` | 商品详情 | 否 |
| 订单 | POST | `/api/v1/orders` | 创建订单 | 是 |
| | GET | `/api/v1/orders/{id}` | 订单详情 | 是 |
| | GET | `/api/v1/orders` | 用户订单列表 | 是 |
| | POST | `/api/v1/orders/{id}/cancel` | 取消订单 | 是 |
| 支付 | POST | `/api/v1/payment/webhook/{gateway}` | 支付回调接收 | 否（由网关签名） |
| | GET | `/api/v1/payment/status/{orderNo}` | 查询支付状态 | 是 |
| 评论 | POST | `/api/v1/comments` | 发表评论/回复 | 是 |
| | GET | `/api/v1/comments` | 获取评论列表（分页+树形） | 否 |
| | GET | `/api/v1/comments/{id}/replies` | 获取某条评论的子回复 | 否 |
| | PUT | `/api/v1/comments/{id}` | 编辑评论（仅本人） | 是 |
| | DELETE | `/api/v1/comments/{id}` | 删除评论（本人或管理员） | 是 |
| | POST | `/api/v1/comments/{id}/like` | 点赞/取消点赞 | 是 |
| | POST | `/api/v1/comments/{id}/dislike` | 点踩/取消点踩 | 是 |
| 弹幕 | POST | `/api/v1/danmaku` | 发送弹幕 | 是 |
| | GET | `/api/v1/danmaku` | 获取指定时间段的弹幕 | 否 |
| | POST | `/api/v1/danmaku/{id}/like` | 弹幕点赞 | 是 |
| | DELETE | `/api/v1/danmaku/{id}` | 删除弹幕（本人或管理员） | 是 |
| 权益 | GET | `/api/v1/entitlements/check` | 检查用户是否有权限访问某内容 | 是 |
| | GET | `/api/v1/entitlements/list` | 获取用户所有权益 | 是 |
| 管理-上传 | POST | `/admin/api/v1/upload/image` | 上传图片（封面/海报） | 是（ADMIN/EDITOR） |
| | POST | `/admin/api/v1/upload/video` | 上传视频（剧集/预告片） | 是（ADMIN/EDITOR） |
| 管理-内容 | POST | `/admin/api/v1/contents` | 创建剧集/影游 | 是（ADMIN/EDITOR） |
| | PUT | `/admin/api/v1/contents/{id}` | 更新剧集/影游信息 | 是（ADMIN/EDITOR） |
| | DELETE | `/admin/api/v1/contents/{id}` | 删除剧集/影游（软删除） | 是（ADMIN） |
| | GET | `/admin/api/v1/contents` | 获取内容列表（分页+筛选） | 是（ADMIN/EDITOR/VIEWER） |
| | GET | `/admin/api/v1/contents/{id}` | 获取内容详情 | 是（ADMIN/EDITOR/VIEWER） |
| | PUT | `/admin/api/v1/contents/{id}/status` | 上下架切换 | 是（ADMIN/EDITOR） |
| 管理-剧集 | POST | `/admin/api/v1/episodes` | 添加单集/关卡 | 是（ADMIN/EDITOR） |
| | PUT | `/admin/api/v1/episodes/{id}` | 更新单集/关卡信息 | 是（ADMIN/EDITOR） |
| | DELETE | `/admin/api/v1/episodes/{id}` | 删除单集/关卡 | 是（ADMIN） |
| | POST | `/admin/api/v1/episodes/batch` | 批量添加剧集 | 是（ADMIN/EDITOR） |
| | PUT | `/admin/api/v1/episodes/sort` | 调整剧集播放顺序 | 是（ADMIN/EDITOR） |

### 5.4 评论与弹幕模块业务设计

#### 5.4.1 评论类型与场景

| 评论类型 | 位置 | 特点 | 用途 |
| :--- | :--- | :--- | :--- |
| **剧集整体评论** | 剧集详情页（简介下方） | 针对整部剧的评价，通常较长、有深度 | 帮助其他用户判断是否值得观看 |
| **单集评论** | 播放页（视频下方） | 类似抖音评论区，针对当前剧集内容 | 实时讨论剧情，增强观看氛围 |
| **弹幕评论** | 播放器上层（叠加） | 短小、实时、飘过画面 | 营造"一起看"的沉浸式互动体验 |

所有评论类型均支持**无限级嵌套回复**（楼中楼），通过 `parent_id` + `root_id` 实现。

#### 5.4.2 评论列表查询策略（构建评论树）

对于"剧集整体评论"或"单集评论"，按时间倒序展示一级评论，每条评论下方显示其最近几条子回复。

**查询流程：**

```
1. 分页查询一级评论 (parent_id = 0) 按 created_at DESC
2. 对每条一级评论，查询其前3条子回复（仅当 reply_count > 0）
3. 更多子回复通过"查看更多回复"接口分页加载
```

**核心SQL — 一级评论列表：**

```sql
SELECT c.*, u.nickname, u.avatar_url
FROM comments c
LEFT JOIN sys_user u ON c.user_id = u.id
WHERE c.content_id = #{contentId}
  AND c.episode_id #{episodeCondition}  -- NULL或具体剧集ID
  AND c.parent_id = 0
  AND c.status = 1
ORDER BY c.created_at DESC
LIMIT #{offset}, #{limit}
```

**核心SQL — 批量查询子回复：**

```sql
SELECT c.*, u.nickname, u.avatar_url, ru.nickname AS reply_to_nickname
FROM comments c
LEFT JOIN sys_user u ON c.user_id = u.id
LEFT JOIN sys_user ru ON c.reply_to_user_id = ru.id
WHERE c.root_id IN (...)
  AND c.parent_id != 0
  AND c.status = 1
ORDER BY c.created_at ASC
LIMIT #{limit}
```

#### 5.4.3 评论发布流程

```java
@Transactional
public CommentVO publishComment(CommentRequest request, Long userId) {
    // 1. 敏感词过滤 (接入第三方服务或本地词库)
    if (sensitiveWordFilter.contains(request.getContent())) {
        throw new BusinessException("评论包含敏感词");
    }
    
    // 2. 评论频率限制 (1分钟最多5条)
    String rateKey = "comment:rate:" + userId;
    if (redisTemplate.opsForValue().increment(rateKey) > 5) {
        throw new BusinessException("评论过于频繁，请稍后再试");
    }
    redisTemplate.expire(rateKey, 60, TimeUnit.SECONDS);
    
    // 3. 构建评论实体
    Comment comment = new Comment();
    comment.setContentId(request.getContentId());
    comment.setEpisodeId(request.getEpisodeId());  // NULL=整剧评论
    comment.setUserId(userId);
    comment.setContent(request.getContent());
    
    Long parentId = request.getParentId() != null ? request.getParentId() : 0L;
    comment.setParentId(parentId);
    
    // 4. 确定root_id和reply_to_user_id
    if (parentId != 0) {
        Comment parentComment = commentMapper.selectById(parentId);
        comment.setRootId(parentComment.getRootId() == 0 ? parentId : parentComment.getRootId());
        comment.setReplyToUserId(parentComment.getUserId());
        
        // 父评论回复数+1
        commentMapper.incrementReplyCount(parentId);
    } else {
        comment.setRootId(0L);
    }
    
    // 5. 保存并返回VO
    commentMapper.insert(comment);
    return convertToVO(comment);
}
```

#### 5.4.4 点赞/点踩逻辑（幂等处理）

同一用户对同一评论只有一条记录，通过 `uk_comment_user` 唯一约束保证：

- **重复相同操作** → 取消（删除记录，计数-1）
- **切换操作** → 更新 type（点赞→点踩，计数联动变更）
- **新增操作** → 插入记录（计数+1）

#### 5.4.5 弹幕查询优化

弹幕是高频查询（每几秒请求一次），必须使用缓存 + 分段加载：

```java
public List<DanmakuVO> getDanmaku(Long episodeId, Long userId, Integer startTime, Integer endTime) {
    // 1. 缓存Key: danmaku:{episodeId}:{startTime}:{endTime}
    String cacheKey = String.format("danmaku:%d:%d:%d", episodeId, startTime, endTime);
    
    String cached = redisTemplate.opsForValue().get(cacheKey);
    if (StringUtils.hasText(cached)) {
        return JSON.parseArray(cached, DanmakuVO.class);
    }
    
    // 2. 从DB查询
    List<Danmaku> danmakuList = danmakuMapper.selectByTimeRange(episodeId, startTime, endTime);
    
    // 3. 写入缓存 (30秒过期，保证弹幕实时性)
    redisTemplate.opsForValue().set(cacheKey, JSON.toJSONString(danmakuList), 30, TimeUnit.SECONDS);
    
    return danmakuList.stream().map(this::convertToVO).collect(Collectors.toList());
}
```

#### 5.4.6 弹幕频率控制

```java
// 单用户发送频率: 3条/10秒
String sendKey = "danmaku:send:" + userId + ":" + episodeId;
Long count = redisTemplate.opsForValue().increment(sendKey);
if (count > 3) {
    throw new BusinessException("弹幕发送过于频繁");
}
if (count == 1) {
    redisTemplate.expire(sendKey, 10, TimeUnit.SECONDS);
}
```

#### 5.4.7 性能优化策略

| 问题场景 | 解决方案 |
| :--- | :--- |
| 评论列表深度嵌套 | 两次查询 + 内存组装，不递归查询数据库 |
| 高并发弹幕请求 | Redis缓存 + 多级分段（每5秒一段），减少DB压力 |
| 评论点赞计数 | Redis原子操作 + 定时同步到DB |
| 敏感词过滤 | 使用DFA算法本地过滤，或接入第三方API |
| 评论排序 | 支持"最新"、"最热"（按点赞数）、"精华"（管理员标记） |

#### 5.4.8 管理端功能

| 功能 | 说明 |
| :--- | :--- |
| 评论管理 | 列表查看、删除违规评论、置顶精华评论 |
| 弹幕管理 | 敏感词过滤、批量删除、屏蔽用户弹幕 |
| 举报处理 | 查看举报列表，一键删除并通知用户 |

#### 5.4.9 前端集成建议（Nuxt.js）

**评论组件结构：**

```
├── CommentSection.vue          # 评论区块 (整合整剧+单集)
│   ├── CommentInput.vue        # 评论输入框 (支持@提及)
│   ├── CommentList.vue         # 评论列表
│   │   ├── CommentItem.vue     # 单条评论 (一级)
│   │   │   └── SubCommentList.vue  # 子回复列表 (折叠/展开)
│   │   │       └── SubCommentItem.vue
│   └── CommentPagination.vue   # 分页
```

**弹幕播放器集成（使用 ArtPlayer）：**

```javascript
const player = new Artplayer({
    container: '#player',
    url: videoUrl,
    danmuku: {
        enabled: true,
        url: '/api/v1/danmaku',
        maxLength: 50,
        filter: (danmaku) => {
            return danmaku; // 本地过滤敏感词
        }
    }
});

// 发送弹幕
player.danmaku.send({
    text: '太好看了！',
    time: player.currentTime,
    color: '#FF0000'
});
```

### 5.5 管理后台——文件上传与剧集管理

#### 5.5.1 设计目标

为管理员提供完整的运营后台API，实现以下操作全程无需接触服务器或数据库：
- 创建/编辑/删除短剧或影游
- 上传和管理剧集/关卡视频
- 配置付费策略（免费/付费）
- 控制内容上下架状态
- 管理封面图、预告片等媒体资源

**设计原则**：

| 原则 | 说明 |
| :--- | :--- |
| **事务一致性** | 涉及多表操作（如创建内容+批量添加剧集）必须使用数据库事务 |
| **异步解耦** | 视频转码、封面图裁剪等耗时操作通过异步任务处理，不阻塞管理员操作 |
| **权限隔离** | 管理端API与用户端API分离，通过独立的认证和权限体系控制 |
| **操作可追溯** | 记录管理员的关键操作日志到 `admin_logs` 表，便于审计 |

#### 5.5.2 文件上传

##### 图片上传

```
POST /admin/api/v1/upload/image
Content-Type: multipart/form-data
```

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| file | File | 是 | 图片文件（jpg/png/webp，最大10MB） |
| type | String | 是 | 用途：cover(封面) / banner(轮播) / avatar(头像) |

**处理流程**：

```
1. 校验文件格式和MIME类型
2. 生成唯一文件名（时间戳+UUID）
3. 按日期分目录存储（如 /2026/01/15/）
4. 上传至云存储（OSS/S3）
5. 封面图异步生成缩略图
6. 记录操作日志
7. 返回访问URL
```

##### 视频上传

```
POST /admin/api/v1/upload/video
Content-Type: multipart/form-data
```

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| file | File | 是 | 视频文件（mp4/mov/avi，最大2GB） |
| episodeId | Long | 否 | 关联剧集ID（上传时即绑定） |

**处理流程**：

```
1. 校验文件格式和大小
2. 生成唯一文件名并上传至云存储（临时目录）
3. 触发异步转码任务（转码为HLS: .m3u8 + .ts）
4. 返回"转码中"状态
5. 转码完成后回调更新 episodes.video_url 和 transcode_status
```

**视频转码异步处理（核心）**：

```java
@Component
public class VideoTranscodeService {

    @Async("videoTaskExecutor")
    public void transcodeVideo(String sourceKey, Long episodeId) {
        try {
            // 1. 更新状态为"转码中"
            episodeService.updateTranscodeStatus(episodeId, 1);

            // 2. 调用云转码服务（阿里云MPS / AWS MediaConvert）
            String outputKey = sourceKey.replace("/raw/", "/hls/").replace(".mp4", ".m3u8");
            String playUrl = mpsService.submitTranscodeJob(sourceKey, outputKey);

            // 3. 更新剧集播放地址
            episodeService.updateVideoUrl(episodeId, playUrl);
            episodeService.updateTranscodeStatus(episodeId, 2);

        } catch (Exception e) {
            log.error("视频转码失败: episodeId={}", episodeId, e);
            episodeService.updateTranscodeStatus(episodeId, 3);
        }
    }
}
```

#### 5.5.3 内容管理（管理端）

##### 创建内容

```java
@Transactional
@PostMapping("/admin/api/v1/contents")
@PreAuthorize("hasRole('ADMIN') or hasRole('EDITOR')")
public Result<Long> createContent(@Valid @RequestBody CreateContentRequest request) {
    Long adminId = getCurrentAdminId();

    // 1. 检查标题重复
    if (contentService.existsByTitle(request.getTitle())) {
        throw new BusinessException("剧集标题已存在");
    }

    // 2. 构建实体并保存
    Content content = buildContent(request, adminId);
    contentMapper.insert(content);

    // 3. 记录操作日志
    adminLogService.log(adminId, "CREATE", "CONTENT",
        String.valueOf(content.getId()), null, content);

    return Result.success(content.getId());
}
```

##### 更新内容（含前置检查）

```java
@Transactional
@PutMapping("/admin/api/v1/contents/{id}")
@PreAuthorize("hasRole('ADMIN') or hasRole('EDITOR')")
public Result<Void> updateContent(@PathVariable Long id, @RequestBody UpdateContentRequest request) {
    Content existing = contentMapper.selectById(id);
    if (existing == null) throw new BusinessException("内容不存在");

    // 保存修改前快照
    String beforeJson = JSON.toJSONString(existing);

    // 更新字段
    BeanUtils.copyProperties(request, existing, "id", "createdBy", "createdAt");
    existing.setUpdatedBy(getCurrentAdminId());
    contentMapper.updateById(existing);

    // 上架检查：必须至少有一个剧集
    if (request.getStatus() == 1 && episodeMapper.countByContentId(id) == 0) {
        throw new BusinessException("该内容尚无剧集，无法上架");
    }

    // 记录日志 + 清除缓存
    adminLogService.log(adminId, "UPDATE", "CONTENT", String.valueOf(id), beforeJson, existing);
    redisTemplate.delete("content:detail:" + id);
    return Result.success();
}
```

##### 软删除

```java
@DeleteMapping("/admin/api/v1/contents/{id}")
@PreAuthorize("hasRole('ADMIN')")
public Result<Void> deleteContent(@PathVariable Long id) {
    // 软删除：status = -1，同步软删除关联剧集
    Content content = new Content();
    content.setId(id);
    content.setStatus(-1);
    content.setUpdatedBy(getCurrentAdminId());
    contentMapper.updateById(content);
    episodeMapper.softDeleteByContentId(id);
    return Result.success();
}
```

#### 5.5.4 剧集管理

##### 批量添加剧集（事务保护）

```java
@Transactional
@PostMapping("/admin/api/v1/episodes/batch")
@PreAuthorize("hasRole('ADMIN') or hasRole('EDITOR')")
public Result<Integer> batchAddEpisodes(@Valid @RequestBody BatchAddRequest request) {
    Long adminId = getCurrentAdminId();

    // 1. 校验关联内容存在
    Content content = contentMapper.selectById(request.getContentId());
    if (content == null || content.getStatus() == -1) {
        throw new BusinessException("关联内容不存在");
    }

    // 2. 批量构建 + 批量插入
    List<Episode> episodes = request.getEpisodes().stream()
        .map(dto -> buildEpisode(request.getContentId(), dto, adminId))
        .collect(Collectors.toList());
    int inserted = episodeMapper.batchInsert(episodes);

    // 3. 更新 content.updated_at
    content.setUpdatedBy(adminId);
    contentMapper.updateById(content);

    // 4. 记录日志 + 清除缓存
    adminLogService.log(adminId, "BATCH_CREATE", "EPISODE",
        String.valueOf(request.getContentId()), null, Map.of("count", inserted));

    return Result.success(inserted);
}
```

##### 调整排序

```
PUT /admin/api/v1/episodes/sort
```

```json
{
    "contentId": 10001,
    "episodeOrders": [
        {"episodeId": 3001, "sortOrder": 1},
        {"episodeId": 3002, "sortOrder": 2}
    ]
}
```

> 批量更新 `sort_order` 字段，清除剧集列表缓存。

#### 5.5.5 幂等性防护

对于可能因网络重试导致重复提交的场景（如重复创建内容），使用 Redis 实现请求去重：

```java
@Transactional
public Long createContent(CreateContentRequest request) {
    String idempotentKey = request.getIdempotentKey();
    if (StringUtils.hasText(idempotentKey)) {
        String cacheKey = "idempotent:createContent:" + idempotentKey;
        Boolean success = redisTemplate.opsForValue()
            .setIfAbsent(cacheKey, "1", 5, TimeUnit.MINUTES);
        if (Boolean.FALSE.equals(success)) {
            return contentService.findByTitle(request.getTitle()).getId();
        }
    }
    // ... 正常创建逻辑
}
```

---

## 6. 支付模块设计（重点）

### 6.1 整体架构

支付模块采用策略模式 + 工厂模式设计，便于扩展多种支付渠道。

**类结构设计：**

```
com.project.module.payment
├── PaymentService (统一接口)
│   ├── createPayment(CreatePaymentRequest req) → PaymentResult
│   ├── handleWebhook(WebhookPayload payload) → void
│   ├── queryPaymentStatus(String gatewayTxId) → PaymentStatus
│   └── refundPayment(String gatewayTxId) → RefundResult
├── strategy
│   ├── PaymentStrategy (接口)
│   ├── PayPalPaymentStrategy (PayPal实现)
│   └── StripePaymentStrategy (Stripe实现)
├── factory
│   └── PaymentStrategyFactory (根据支付方式创建策略)
├── webhook
│   ├── WebhookHandler (接口)
│   ├── PayPalWebhookHandler
│   └── StripeWebhookHandler
└── dto
    ├── CreatePaymentRequest
    ├── PaymentResult
    └── WebhookPayload
```

### 6.2 支付核心流程

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   用户端     │    │  后端服务    │    │  支付网关   │
│  (Nuxt.js)  │    │ (Java)      │    │(PayPal/Stripe)│
└──────┬──────┘    └──────┬──────┘    └──────┬──────┘
       │                  │                  │
       │ 1. 选择商品      │                  │
       │─────────────────>│                  │
       │                  │ 2. 创建订单      │
       │                  │ (状态:待支付)    │
       │                  │                  │
       │                  │ 3. 请求支付参数  │
       │                  │─────────────────>│
       │                  │                  │
       │                  │ 4. 返回支付参数  │
       │                  │ (redirect_url / │
       │                  │  client_secret) │
       │ 5. 返回支付参数  │                  │
       │<─────────────────│                  │
       │                  │                  │
       │ 6. 跳转支付网关完成支付              │
       │─────────────────────────────────────>│
       │                  │                  │
       │                  │ 7. Webhook回调   │
       │                  │<─────────────────│
       │                  │                  │
       │                  │ 8. 签名验证      │
       │                  │ 9. 幂等性处理    │
       │                  │10. 更新订单状态  │
       │                  │11. 发放用户权益  │
       │                  │                  │
       │ 12. 支付完成页面 │                  │
       │<─────────────────│                  │
       │                  │                  │
```

### 6.3 Webhook安全关键点

#### 6.3.1 签名验证（必须实现）

支付网关发送的Webhook请求会携带签名头，后端必须验证其真实性，防止伪造回调：

```java
// PayPal示例
boolean isValid = PayPalWebhookVerifier.verify(
    webhookId,
    requestBody,
    requestHeaders.get("PayPal-Transmission-Sig")
);

// Stripe示例
boolean isValid = StripeWebhook.constructEvent(
    requestBody,
    requestHeaders.get("Stripe-Signature"),
    webhookSecret
);
```

#### 6.3.2 幂等性处理（必须实现）

同一笔支付可能因网络重试导致多次回调，必须根据`gateway_order_id`或`gateway_tx_id`做幂等判断：

```java
@Transactional
public void handleWebhook(String gatewayTxId, PaymentStatus status) {
    // 1. 根据gateway_tx_id查询是否已处理
    Orders existingOrder = orderService.findByGatewayTxId(gatewayTxId);
    if (existingOrder != null && existingOrder.getStatus() == PAID) {
        log.info("订单 {} 已处理，忽略重复回调", existingOrder.getOrderNo());
        return;
    }
    
    // 2. 更新订单状态
    orderService.updateStatus(gatewayTxId, status);
    
    // 3. 发放用户权益（分布式锁防止并发）
    String lockKey = "payment:entitlement:" + gatewayTxId;
    RLock lock = redissonClient.getLock(lockKey);
    try {
        if (lock.tryLock(10, 30, TimeUnit.SECONDS)) {
            entitlementService.grant(userId, productId);
        }
    } finally {
        if (lock.isHeldByCurrentThread()) {
            lock.unlock();
        }
    }
}
```

#### 6.3.3 异步处理建议

Webhook处理应放入消息队列异步执行，避免阻塞网关响应（超时可能导致重试）：

```java
@RestController
public class WebhookController {
    
    @PostMapping("/api/v1/payment/webhook/{gateway}")
    public ResponseEntity<String> handleWebhook(
            @PathVariable String gateway,
            @RequestBody String payload,
            @RequestHeader Map<String, String> headers) {
        
        // 1. 快速验证签名（同步）
        if (!webhookValidator.validate(gateway, payload, headers)) {
            return ResponseEntity.status(401).body("Invalid signature");
        }
        
        // 2. 异步处理业务逻辑
        paymentEventPublisher.publish(new WebhookEvent(gateway, payload));
        
        // 3. 快速返回200（网关收到200即停止重试）
        return ResponseEntity.ok("OK");
    }
}
```

### 6.4 支付配置管理

支付相关敏感配置通过环境变量注入，不写入代码仓库：

```yaml
# application-prod.yml
payment:
  paypal:
    client-id: ${PAYPAL_CLIENT_ID}
    client-secret: ${PAYPAL_CLIENT_SECRET}
    webhook-secret: ${PAYPAL_WEBHOOK_SECRET}
    mode: ${PAYPAL_MODE:live}  # sandbox / live
  stripe:
    secret-key: ${STRIPE_SECRET_KEY}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET}
```

---

## 7. 安全设计

### 7.1 认证与授权

#### 7.1.1 JWT无状态认证

```
用户登录 → 验证凭证 → 生成JWT Token → 返回给前端
后续请求 → 携带Token → 后端验证签名 → 解析用户信息 → 处理业务
```

**Token设计：**

```json
{
  "sub": "user_id",
  "username": "test@example.com",
  "roles": ["USER"],
  "iat": 1704067200,
  "exp": 1704074400  // 有效期2小时
}
```

**安全配置：**

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf().disable()
            .cors().configurationSource(corsConfigurationSource())
            .and()
            .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            .and()
            .authorizeHttpRequests()
                .requestMatchers("/api/v1/auth/**", "/api/v1/contents/**").permitAll()
                .requestMatchers("/api/v1/payment/webhook/**").permitAll()
                .anyRequest().authenticated()
            .and()
            .addFilterBefore(jwtAuthenticationFilter(), UsernamePasswordAuthenticationFilter.class);
        
        return http.build();
    }
}
```

#### 7.1.2 管理员权限控制

基于RBAC模型，使用`@PreAuthorize`注解控制接口权限：

```java
@RestController
@RequestMapping("/api/v1/admin/contents")
public class AdminContentController {
    
    @PostMapping
    @PreAuthorize("hasRole('ADMIN') or hasRole('EDITOR')")
    public Result<Void> createContent(@RequestBody ContentCreateRequest req) {
        // ...
    }
    
    @DeleteMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public Result<Void> deleteContent(@PathVariable Long id) {
        // ...
    }
}
```

### 7.2 数据传输安全

| 安全措施 | 说明 |
| :--- | :--- |
| 强制HTTPS | 生产环境全站启用TLS 1.3，通过Nginx配置SSL证书 |
| 密码加密 | 使用BCrypt加密存储（强度系数≥10） |
| 敏感信息脱敏 | 日志中自动脱敏手机号、邮箱等敏感字段 |
| 支付敏感数据 | 卡号等不经过后端，通过Stripe.js/PayPal SDK直接发送至网关 |

### 7.3 防护策略

| 攻击类型 | 防护措施 |
| :--- | :--- |
| SQL注入 | MyBatis-Plus参数化查询，禁止SQL拼接 |
| XSS攻击 | 接口层对用户输入进行HTML转义过滤 |
| CSRF攻击 | 使用JWT无状态认证，天然免疫CSRF |
| 暴力破解 | 登录接口限流（5次/分钟），超过后锁定IP 30分钟 |
| DDoS攻击 | 通过云服务商WAF + Nginx限流模块防护 |
| 重复支付 | 分布式锁 + 订单状态机防止重复支付 |
| 文件上传攻击 | 文件类型白名单（Magic Number校验）、大小限制、UUID重命名防路径遍历 |
| 重复提交 | Redis幂等键（idempotentKey）5分钟去重 |

### 7.4 文件上传安全

| 防护措施 | 说明 |
| :--- | :--- |
| 文件类型白名单 | 仅允许指定格式：jpg/png/webp（图片）、mp4/mov/avi（视频） |
| MIME类型校验 | 不仅校验扩展名，还校验文件头Magic Number |
| 文件大小限制 | 图片 ≤ 10MB，视频 ≤ 2GB |
| 文件名净化 | 使用UUID重命名，防止路径遍历攻击 |
| 病毒扫描 | 可选：接入云安全服务扫描上传文件 |

### 7.5 操作审计

所有管理端关键操作（创建、编辑、删除、上下架、上传）均记录到 `admin_logs` 表：

| 审计字段 | 说明 |
| :--- | :--- |
| 操作人 | `admin_id` 关联管理员 |
| 操作时间 | `created_at` 精确到秒 |
| 操作IP | `ip_address` 用于安全追溯 |
| 操作类型 | `action`：CREATE / UPDATE / DELETE / UPLOAD |
| 操作模块 | `module`：CONTENT / EPISODE / UPLOAD |
| 数据快照 | `before_data` / `after_data`（JSON格式），支持误操作恢复 |

### 7.6 接口限流配置

使用Redis + 令牌桶算法实现限流：

```java
@Component
public class RateLimitService {
    
    @Autowired
    private StringRedisTemplate redisTemplate;
    
    public boolean allowRequest(String key, int limit, int period) {
        String luaScript = """
            local current = redis.call('incr', KEYS[1])
            if current == 1 then
                redis.call('expire', KEYS[1], ARGV[1])
            end
            return current <= tonumber(ARGV[2])
            """;
        return redisTemplate.execute(
            new DefaultRedisScript<>(luaScript, Boolean.class),
            List.of("ratelimit:" + key),
            String.valueOf(period),
            String.valueOf(limit)
        );
    }
}
```

---

## 8. 性能与扩展性设计

### 8.1 缓存策略

| 缓存场景 | 数据类型 | TTL | 更新策略 |
| :--- | :--- | :--- | :--- |
| 热门内容列表 | Redis Hash | 5分钟 | 定时任务刷新 + 后台变更时主动失效 |
| 内容详情 | Redis String (JSON) | 30分钟 | 查看时懒加载写入，变更时主动删除 |
| 用户会话 (JWT黑名单) | Redis String | Token有效期 | 登出或Token过期时写入 |
| 用户权益信息 | Redis Hash | 10分钟 | 查看时懒加载，支付成功后主动刷新 |
| 分布式锁 | Redis (Redisson) | 自动续期 | 业务执行完主动释放 |
| 接口限流计数 | Redis String | 时间窗口 | 自动过期 |

### 8.2 数据库优化

#### 8.2.1 索引设计原则

- 为所有外键字段建索引（`user_id`, `content_id`, `product_id`）
- 为高频查询字段建索引（`status`, `type`, `created_at`）
- 避免在低选择性字段（如`type`只有1/2）上建过多索引

#### 8.2.2 分页查询优化

对于大表分页，避免使用 `OFFSET + LIMIT` 深度翻页：

```sql
-- 不推荐（OFFSET过大时性能差）
SELECT * FROM contents WHERE status = 1 ORDER BY id LIMIT 100000, 20;

-- 推荐（使用游标方式）
SELECT * FROM contents WHERE status = 1 AND id > #{lastId} ORDER BY id LIMIT 20;
```

#### 8.2.3 读写分离（流量增长后）

配置MySQL主从同步，读操作走从库：

```java
@Configuration
public class DataSourceConfig {
    
    @Bean
    @Primary
    public DataSource masterDataSource() { /* 主库 */ }
    
    @Bean
    public DataSource slaveDataSource() { /* 从库 */ }
    
    @Bean
    public DataSource routingDataSource() {
        // 使用AbstractRoutingDataSource实现读写分离
    }
}
```

### 8.3 扩展性预留

| 扩展方向 | 预留设计 |
| :--- | :--- |
| 微服务拆分 | 包结构按模块划分，接口层独立，后续可拆分为独立服务 |
| 异步任务 | 预留消息队列接口（RabbitMQ/Kafka），视频转码等异步任务可后续接入 |
| 多语言内容 | contents表预留字段，content_extras可存储多语言版本 |
| 推荐系统 | 用户行为数据单独建表存储，为后续推荐算法提供数据基础 |
| CDN切换 | 视频URL通过配置中心管理，可无感切换CDN服务商 |

---

## 9. 项目目录结构

```
project-backend/
├── src/main/java/com/project/
│   ├── ProjectApplication.java              # Spring Boot启动类
│   │
│   ├── common/                              # 公共模块
│   │   ├── config/                          # 配置类
│   │   │   ├── RedisConfig.java
│   │   │   ├── SwaggerConfig.java
│   │   │   ├── CorsConfig.java
│   │   │   └── WebMvcConfig.java
│   │   ├── exception/                       # 全局异常处理
│   │   │   ├── GlobalExceptionHandler.java
│   │   │   ├── BusinessException.java
│   │   │   └── ErrorCode.java
│   │   ├── response/                        # 统一响应封装
│   │   │   ├── Result.java
│   │   │   ├── PageResult.java
│   │   │   └── ResultCode.java
│   │   ├── utils/                           # 工具类
│   │   │   ├── JwtUtil.java
│   │   │   ├── EncryptionUtil.java
│   │   │   └── DateUtil.java
│   │   └── annotation/                      # 自定义注解
│   │       ├── RequirePermission.java
│   │       └── RateLimit.java
│   │
│   ├── module/                              # 业务模块
│   │   ├── user/                            # 用户模块
│   │   │   ├── controller/
│   │   │   │   ├── AuthController.java
│   │   │   │   └── UserController.java
│   │   │   ├── service/
│   │   │   │   ├── UserService.java
│   │   │   │   └── impl/UserServiceImpl.java
│   │   │   ├── mapper/UserMapper.java
│   │   │   ├── entity/
│   │   │   │   ├── SysUser.java
│   │   │   │   └── SysAdmin.java
│   │   │   └── dto/
│   │   │       ├── LoginRequest.java
│   │   │       ├── RegisterRequest.java
│   │   │       └── UserProfileVO.java
│   │   │
│   │   ├── content/                         # 内容模块
│   │   │   ├── controller/
│   │   │   │   ├── ContentController.java
│   │   │   │   └── EpisodeController.java
│   │   │   ├── service/
│   │   │   │   ├── ContentService.java
│   │   │   │   ├── EpisodeService.java
│   │   │   │   └── impl/
│   │   │   ├── mapper/
│   │   │   │   ├── ContentMapper.java
│   │   │   │   ├── EpisodeMapper.java
│   │   │   │   └── ContentExtrasMapper.java
│   │   │   ├── entity/
│   │   │   │   ├── Content.java
│   │   │   │   ├── Episode.java
│   │   │   │   └── ContentExtras.java
│   │   │   └── dto/
│   │   │       ├── ContentDetailVO.java
│   │   │       └── EpisodePlayVO.java
│   │   │
│   │   ├── product/                         # 商品模块
│   │   │   ├── controller/ProductController.java
│   │   │   ├── service/ProductService.java
│   │   │   ├── mapper/ProductMapper.java
│   │   │   └── entity/Product.java
│   │   │
│   │   ├── order/                           # 订单模块
│   │   │   ├── controller/OrderController.java
│   │   │   ├── service/OrderService.java
│   │   │   ├── mapper/OrderMapper.java
│   │   │   ├── entity/Order.java
│   │   │   └── dto/CreateOrderRequest.java
│   │   │
│   │   ├── payment/                         # 支付模块
│   │   │   ├── controller/PaymentController.java
│   │   │   ├── service/
│   │   │   │   ├── PaymentService.java
│   │   │   │   └── impl/PaymentServiceImpl.java
│   │   │   ├── strategy/
│   │   │   │   ├── PaymentStrategy.java
│   │   │   │   ├── PayPalPaymentStrategy.java
│   │   │   │   └── StripePaymentStrategy.java
│   │   │   ├── factory/PaymentStrategyFactory.java
│   │   │   ├── webhook/
│   │   │   │   ├── WebhookHandler.java
│   │   │   │   ├── PayPalWebhookHandler.java
│   │   │   │   └── StripeWebhookHandler.java
│   │   │   └── dto/
│   │   │       ├── CreatePaymentRequest.java
│   │   │       ├── PaymentResult.java
│   │   │       └── WebhookPayload.java
│   │   │
│   │   ├── entitlement/                     # 权益模块
│   │   │   ├── controller/EntitlementController.java
│   │   │   ├── service/EntitlementService.java
│   │   │   ├── mapper/EntitlementMapper.java
│   │   │   └── entity/UserEntitlement.java
│   │   │
│   │   ├── social/                          # 评论与弹幕模块
│   │   │   ├── controller/
│   │   │   │   ├── CommentController.java
│   │   │   │   └── DanmakuController.java
│   │   │   ├── service/
│   │   │   │   ├── CommentService.java
│   │   │   │   ├── DanmakuService.java
│   │   │   │   └── impl/
│   │   │   ├── mapper/
│   │   │   │   ├── CommentMapper.java
│   │   │   │   ├── DanmakuMapper.java
│   │   │   │   ├── CommentLikeMapper.java
│   │   │   │   └── DanmakuLikeMapper.java
│   │   │   ├── entity/
│   │   │   │   ├── Comment.java
│   │   │   │   ├── Danmaku.java
│   │   │   │   ├── CommentLike.java
│   │   │   │   └── DanmakuLike.java
│   │   │   └── dto/
│   │   │       ├── CommentVO.java
│   │   │       ├── CommentRequest.java
│   │   │       ├── DanmakuVO.java
│   │   │       └── DanmakuRequest.java
│   │   │
│   │   └── admin/                           # 管理后台API
│   │       ├── controller/
│   │       │   ├── AdminAuthController.java
│   │       │   ├── AdminContentController.java
│   │       │   ├── AdminEpisodeController.java
│   │       │   ├── AdminOrderController.java
│   │       │   └── AdminUserController.java
│   │       ├── service/
│   │       │   ├── AdminAuthService.java
│   │       │   ├── AdminContentService.java
│   │       │   ├── AdminLogService.java          # 操作审计日志
│   │       │   └── impl/
│   │       ├── mapper/
│   │       │   └── AdminLogMapper.java
│   │       ├── entity/
│   │       │   └── AdminLog.java
│   │       └── dto/
│   │
│   ├── infrastructure/                      # 基础设施
│   │       ├── security/                        # 安全配置
│   │       │   ├── JwtAuthenticationFilter.java
│   │       │   ├── JwtTokenProvider.java
│   │       │   └── SecurityConfig.java
│   │       ├── upload/                          # 文件上传服务
│   │       │   ├── ImageUploadService.java      # 图片上传+缩略图
│   │       │   └── VideoUploadService.java      # 视频上传+转码
│   │       ├── cache/                           # 缓存服务
│   │
│   └── infrastructure/                      # 基础设施
│       ├── security/                        # 安全配置
│       │   ├── JwtAuthenticationFilter.java
│       │   ├── JwtTokenProvider.java
│       │   └── SecurityConfig.java
│       ├── cache/                           # 缓存服务
│       │   ├── CacheService.java
│       │   └── RedisCacheService.java
│       └── lock/                            # 分布式锁
│           ├── DistributedLock.java
│           └── RedissonLockService.java
│
├── src/main/resources/
│   ├── application.yml                      # 主配置文件
│   ├── application-dev.yml                  # 开发环境配置
│   ├── application-prod.yml                 # 生产环境配置
│   └── mapper/                              # MyBatis XML映射文件
│       ├── ContentMapper.xml
│       ├── OrderMapper.xml
│       └── ...
│
├── pom.xml                                  # Maven依赖管理
├── Dockerfile                               # 应用容器构建文件
└── docker-compose.yml                       # 容器编排配置
```

---

## 10. 开发顺序与里程碑

### 10.1 开发阶段划分

| 阶段 | 周期 | 核心任务 | 交付物 |
| :--- | :--- | :--- | :--- |
| 第一阶段：基础框架 | 第1-2周 | 项目初始化、Spring Boot集成、MyBatis-Plus配置、Redis集成、Swagger配置、统一响应封装、全局异常处理 | 可运行的空项目，API文档自动生成 |
| 第二阶段：用户认证 | 第3-4周 | JWT实现、注册/登录接口、Spring Security配置、个人资料管理 | 用户可注册登录，获取Token |
| 第三阶段：内容管理 | 第5-6周 | 内容CRUD、剧集管理、内容扩展表（content_extras）读写、内容列表/详情接口 | 内容可通过API管理，前端可展示 |
| 第四阶段：商品与订单 | 第7-8周 | 商品管理、订单创建、订单查询、订单状态管理 | 用户可下单购买商品 |
| 第五阶段：支付模块 | 第9-11周 | PayPal/Stripe沙盒对接、Webhook实现、签名验证、幂等性处理、权益自动发放 | 完成支付全流程闭环测试 |
| 第六阶段：管理后台API | 第12-13周 | 管理员认证、内容管理接口、订单管理接口、数据统计接口 | 管理端可完成运营操作 |
| 第七阶段：评论与弹幕 | 第14-15周 | 评论CRUD、多级回复树形查询、弹幕发送与分段查询、点赞/点踩、Redis缓存优化、管理端评论管理 | 用户可发表评论和弹幕互动 |
| 第八阶段：测试与部署 | 第16-18周 | 全流程联调、沙盒支付测试、安全加固、Docker部署、上线 | 1.0版本正式上线 |

### 10.2 里程碑节点

| 里程碑 | 时间节点 | 验收标准 |
| :--- | :--- | :--- |
| M1：基础框架就绪 | 第2周末 | 项目可本地运行，Swagger可访问 |
| M2：用户认证完成 | 第4周末 | 注册/登录/JWT全流程通过Postman测试 |
| M3：内容管理完成 | 第6周末 | 内容增删改查API全部就绪 |
| M4：支付闭环完成 | 第11周末 | 沙盒环境支付流程跑通（创建订单→支付→回调→权益发放） |
| M5：评论弹幕完成 | 第15周末 | 评论树形查询、弹幕收发、点赞互动全部就绪 |
| M6：正式上线 | 第18周末 | 生产环境部署完成，核心业务可用 |

---

## 11. 部署与运维

### 11.1 部署架构

```
                    ┌─────────────────────┐
                    │   CDN (视频加速)    │
                    └──────────┬──────────┘
                               │
┌──────────────┐    ┌──────────▼──────────┐    ┌──────────────┐
│   用户请求   │───▶│   Nginx (负载均衡)   │───▶│  Spring Boot │
│   (HTTPS)   │    │   + SSL终止 + WAF    │    │  应用集群     │
└──────────────┘    └──────────┬──────────┘    └──────┬───────┘
                               │                      │
                    ┌──────────▼──────────┐  ┌────────▼────────┐
                    │    MySQL 8.0        │  │   Redis 7.0    │
                    │   (主从同步)         │  │   (缓存/会话)   │
                    └─────────────────────┘  └─────────────────┘
```

### 11.2 环境配置建议

| 环境 | 配置 | 说明 |
| :--- | :--- | :--- |
| 开发环境 | 本地MySQL + Redis | 使用application-dev.yml配置 |
| 测试环境 | 云服务器（2C4G）+ 托管数据库 | 用于联调和沙盒支付测试 |
| 生产环境 | 云服务器集群 + 数据库主从 | 需配置监控告警、日志采集 |

### 11.3 Docker Compose 示例

```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: project-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: project_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7.0-alpine
    container_name: project-redis
    ports:
      - "6379:6379"

  app:
    build: .
    container_name: project-app
    depends_on:
      - mysql
      - redis
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_URL: jdbc:mysql://mysql:3306/project_db
      REDIS_HOST: redis
    ports:
      - "8080:8080"

volumes:
  mysql_data:
```

---

## 12. 附录

### 12.1 关键依赖版本

```xml
<!-- pom.xml 关键依赖 -->
<properties>
    <java.version>17</java.version>
    <spring-boot.version>3.1.5</spring-boot.version>
    <mybatis-plus.version>3.5.3.1</mybatis-plus.version>
    <jjwt.version>0.11.5</jjwt.version>
    <knife4j.version>4.1.0</knife4j.version>
    <redisson.version>3.24.3</redisson.version>
</properties>
```

### 12.2 外部服务依赖

| 服务 | 用途 | 官方文档 |
| :--- | :--- | :--- |
| PayPal API | 海外支付 | <https://developer.paypal.com> |
| Stripe API | 海外支付 | <https://stripe.com/docs/api> |
| AWS S3 / 阿里云OSS | 视频/图片存储 | 各自官方文档 |
| CDN服务商 | 视频加速分发 | 根据选型确定 |

### 12.3 参考文档

- Spring Boot官方文档：<https://spring.io/projects/spring-boot>
- MyBatis-Plus官方文档：<https://baomidou.com>
- JWT规范：<https://jwt.io>
- OWASP Top 10安全指南：<https://owasp.org/Top10>

---

**文档版本**：v1.2

**最后更新**：2026-08-07

**文档状态**：已更新（新增管理后台上传、批量剧集管理、操作审计）



